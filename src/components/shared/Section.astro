---
export 
const { title } = Astro.props
---

<section>
  <h1>{title}</h1>
  <slot/>
</section>

<style lang="scss">
  section {
    padding: 50px 30px;
  }
  h1 {
    color: var(--black);
    font-size: 2rem;
    font-weight: 600;
    margin-bottom: 50px;
  }
</style>